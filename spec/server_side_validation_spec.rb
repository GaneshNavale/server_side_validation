require "spec_helper"

describe ServerSideValidation do
  it "has a version number" do
    expect(ServerSideValidation::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(false)
  end
end
